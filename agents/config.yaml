data_dir: "../data"
index_dir: "./index"

llm:
  provider: "openai"
  model: "gpt-4.1-mini"
  temperature: 0.2

embeddings:
  provider: "openai"
  model: "text-embedding-3-small"

ingestion:
  chunk_size: 1200
  chunk_overlap: 200

