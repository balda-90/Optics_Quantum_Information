# Stack per i mini-progetti di quantum computing del master.
# Installazione consigliata in un venv dedicato:
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1
#   pip install -r quantum/requirements.txt

# Core scientifico
# Nota: Qiskit 2.x richiede numpy>=2.0, incompatibile con lo stack RAG
# (langchain) del progetto principale. Per questo il codice quantum vive in
# un venv DEDICATO (vedi README): .venv-quantum
numpy>=2.0
scipy>=1.13
matplotlib>=3.8

# Quantum computing (IBM Qiskit)
qiskit>=1.2
qiskit-aer>=0.15

# Notebook (opzionale, utile per esplorare)
jupyter>=1.0
