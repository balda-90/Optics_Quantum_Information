# Stack for the master's quantum-computing mini-projects.
# Recommended install in a dedicated venv:
#   python -m venv .venv-quantum
#   .venv-quantum\Scripts\Activate.ps1
#   pip install -r quantum/requirements.txt

# Scientific core
# Note: Qiskit 2.x requires numpy>=2.0, which is incompatible with the RAG
# stack (langchain) of the main project. That is why the quantum code lives in
# a DEDICATED venv (see README): .venv-quantum
numpy>=2.0
scipy>=1.13
matplotlib>=3.8
pyyaml>=6.0
pandas>=2.0

# Quantum computing (IBM Qiskit)
qiskit>=1.2
qiskit-aer>=0.15

# Notebook (optional, handy for exploration)
jupyter>=1.0
